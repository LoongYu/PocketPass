# PocketPass｜口袋密码 V1.1 三端功能测试报告

测试日期：2026-08-15

测试版本：`V1.1(2)`

结论：**10 轮审查与回归全部通过；无阻断缺陷，存在已知发布与安全加固项。**

## 1. 测试范围与环境

| 项目 | 环境 |
|---|---|
| 开发主机 | macOS 26.6.1（25G76），Apple Silicon arm64 |
| Xcode | 26.6（17F113） |
| Swift | 6.3.3 |
| iOS 模拟器运行时 | iOS 26.5（23F77） |
| macOS 最低版本 | 26.0 |
| iOS/iPadOS 最低版本 | 26.0 |
| macOS 产物 | `dist/PocketPass-2026081502.dmg` |
| macOS SHA-256 | `bd0b42f7f7ff77e7e6721b144284cf312db5da826418acd07422efa8a788ed89` |

代表性设备矩阵：

| 设备 | 覆盖目的 | 结果 |
|---|---|---|
| iPhone 17 Pro Max | 大尺寸 iPhone、默认目标、首次启动空数据 | 通过 |
| iPhone 17e | 较窄 iPhone、英文换行、亮/暗主题、设备密码认证 | 通过 |
| iPad mini (A17 Pro) | 最紧凑 iPad、纵向分栏、英文与亮色 | 通过 |
| iPad Pro 13-inch (M5) | 最大 iPad、横向分栏和大画布 | 通过 |
| Apple Silicon Mac | macOS 原生界面、Release App、DMG | 通过 |
| Intel Mac 架构 | x86_64 编译、合并和 Mach-O 结构检查 | 通过（静态架构验证） |

SwiftUI 布局按 Compact/Regular size class 自适应，因此同一系统版本下未逐个重复创建所有仅尺寸接近的 iPhone/iPad 型号。上述设备覆盖当前 Apple 手机和平板的最小/最大代表尺寸；真实摄像头安全区、外接键盘、VoiceOver 和每一款物理设备仍属于后续真机专项范围。

## 2. 总体结果

| 类别 | 结果 |
|---|---|
| 10 轮审查 | 10/10 通过 |
| 共享核心自动测试 | 16/16 通过 |
| Swift Package Debug 构建 | 通过 |
| Swift Package Release 构建 | 通过 |
| Xcode 静态分析 | `ANALYZE SUCCEEDED` |
| iPhone 构建 | `BUILD SUCCEEDED` |
| iPad 构建 | `BUILD SUCCEEDED` |
| macOS Universal Binary | `x86_64 arm64` |
| macOS App 签名结构 | `codesign --verify --deep --strict` 通过 |
| DMG 校验 | `hdiutil verify` 返回 VALID |
| 阻断缺陷 | 0 |
| 高优先级未修复功能缺陷 | 0 |

唯一构建提示为 AppIntents 元数据提取跳过，因为当前 target 不依赖 AppIntents；不影响应用功能或安装。

## 3. 十轮审查与测试记录

### 第 1 轮：共享模型与旧数据兼容

结果：**通过**

- 三端统一使用 `PocketPassCore` 中的分类、账户项目、多登录账号、自定义字段、标签、图标和附件模型。
- 验证 `VaultSnapshot.formatVersion = 3`。
- 验证缺失新字段的旧 JSON 可导入，默认补齐网站、标签、字段、收藏和时间信息。
- 验证旧 Markdown 中一个项目包含多个登录账号的解析。

优化：为五个核心模型加入向后兼容解码默认值，避免旧备份因新增字段直接失败。

### 第 2 轮：本地加密与密钥异常安全

结果：**通过**

- 数据保存后，密码库文件中不出现测试密码明文。
- 重新实例化仓库可正确解密并恢复账户。
- 密钥文件长度为 32 字节。
- 删除本地密钥后重新加载会报告错误，原加密密码库字节保持不变，不会被空快照覆盖。

优化：新增 `missingLocalKey` 失败路径，并阻止加载失败后的默认数据持久化。

### 第 3 轮：导入、导出、备份与重复校验

结果：**通过**

- JSON、CSV、Markdown 和 `.pocketpass` 加密备份完成往返。
- 加密备份中不出现密码明文。
- 同一份数据重复合并第二次只计入“跳过重复”，账户总数不增加。
- CSV 自定义字段值包含多个 `=` 时保持完整，秘密字段标记不丢失。
- 同 UUID 数据按最后修改时间较新版本优先。

优化：修正 CSV 字段拆分逻辑，并统一孤立分类修复、附件过滤和图标去重。

### 第 4 轮：账户、分类、标签、回收站和搜索

结果：**通过**

- 新建、编辑、批量移入回收站、恢复和永久删除路径通过。
- 30 天前删除的项目会自动清理并立即持久化。
- 删除分类后所有相关账户迁移到有效分类。
- 标签创建、大小写/宽度归一化、重命名、删除和持久化通过。
- 分类拖拽顺序保存后重新加载保持一致。
- 搜索覆盖标签、自定义字段名和嵌套字段值。

### 第 5 轮：密码生成与共享资源边界

结果：**通过**

- 生成密码长度正确，并确保已启用的大写、数字和符号至少出现一次。
- 连续生成 128 个 20 位密码均不重复。
- 三端长度范围统一为 8–64。
- 每个账户最多 5 张附件，单张最大 2,000,000 字节。
- 账户、分类、自定义图标最大 2,000,000 字节；自定义图标库最多 100 枚并内容去重。
- 无效分类 ID 会在共享核心修复。

优化：将资源上限从平台界面下沉到共享核心，导入或直接调用业务层也无法绕过。

### 第 6 轮：iPhone 大小屏与首次启动

结果：**通过**

- iPhone 17 Pro Max 首次启动显示空账户和六个默认分类，无预置账户数据。
- iPhone 17e 使用测试过程中临时录入的数据验证首页、分类横向滚动、批量管理和浮动新增按钮，无截断。
- 大标题、总览、账户卡片和底部导航适配安全区。

### 第 7 轮：iPad mini 与 iPad Pro 自适应

结果：**通过**

- iPad mini 纵向使用常驻侧栏，首页内容在最窄 iPad 画布内完整显示。
- iPad Pro 13-inch 横向分栏比例合理，分类/账户列表与详情区可同时工作。
- 设置页、内容管理、数据入口和底部“添加账户”在侧栏布局中可访问。
- 同一 target 支持 iPhone 与 iPad device family，无重复业务代码。

### 第 8 轮：深色、亮色、中文与英文

结果：**通过**

- iPhone 深色 English：总览、生成密码、数量复数、账户/分类标题和底部导航完整显示。
- iPhone 亮色简体中文：文字、按钮、卡片边界和品牌色对比度可见。
- iPad mini 亮色 English：侧栏、总览、分类筛选、账户卡片和设置页完整显示。
- 用户自建名称保持原内容，不会错误翻译；默认分类名称会本地化。
- 导入数量、导出数量、图片错误、复制字段和认证说明等运行时提示已接入应用语言。

优化：修正小屏总览字体缩放、英文按钮换行和动态数量文案；About 版本改为读取 Bundle。

### 第 9 轮：应用锁、网络图标与平台系统交互

结果：**通过，含环境说明**

- iPhone 应用锁成功调起系统“为口袋密码输入 iPhone 密码”认证界面。
- 认证理由说明仅访问本地密码库；支持 Face ID 或设备密码策略。
- 自动锁定包含立即、1、5、15、30 分钟。
- App Store 搜索默认中国区并保留九个区域选择。
- 网址 favicon、App Store 图标和本地照片均经过平台图片处理与共享核心二次限制。
- macOS 使用 Touch ID 或设备密码的 LocalAuthentication 策略。

说明：模拟器验证了系统设备密码认证弹窗；Face ID/Touch ID 的真实生物传感器成功与失败路径仍需在物理设备人工确认。

### 第 10 轮：最终静态分析、三端构建与发布产物

结果：**通过**

- `swift test`：16 项通过，0 失败。
- `swift build -c release`：通过。
- `xcodebuild analyze`：通过。
- iPhone 17 Pro Max 与 iPad mini 构建：通过。
- macOS 分别构建 arm64、x86_64 后使用 `lipo` 合并。
- 最终 App 版本：`CFBundleShortVersionString=1.1`、`CFBundleVersion=2`。
- 最终 App 包含 `x86_64 arm64`，签名结构验证通过。
- DMG 挂载后包含“口袋密码.app”与指向 `/Applications` 的快捷方式。
- DMG 校验和有效，包内不包含本地密码库或测试账户。
- 最终 macOS App 可启动为运行中进程；之前同一 Release 包界面已完成可视冒烟验证。

## 4. 共享自动测试明细

1. 加密持久化与重新加载。
2. 回收站恢复与永久删除。
3. 重复导入幂等。
4. 密码生成字符约束与随机性抽样。
5. JSON/CSV/Markdown/加密备份往返。
6. 旧 Markdown 多登录账号导入。
7. 密钥缺失不覆盖密码库。
8. 过期回收站清理持久化。
9. 导入孤立分类修复和附件限制。
10. 删除分类后账户迁移。
11. 标签与嵌套字段搜索。
12. 旧 JSON 缺失字段兼容。
13. CSV 等号值与秘密字段兼容。
14. UI 外调用也执行资源上限。
15. 标签与分类排序持久化。
16. 自定义图标去重与持久化。

## 5. 已知限制与非阻断风险

### 5.1 macOS 分发

当前 App 仅使用临时签名，没有 Developer ID 和 Apple 公证。其他 Mac 可能被 Gatekeeper 拦截，需要右键打开或在“隐私与安全性”中允许。该项不影响本机开发验证，但公开分发前必须解决。

### 5.2 本地密钥保护

密码库已加密，但本地密钥与密码库处于同一应用数据域；尚未由钥匙串或 Secure Enclave 封装。当前强度适合个人本地版本，不应宣传为经过独立审计的专业密码管理器。

### 5.3 未实现范围

- iCloud/CloudKit 同步。
- 浏览器扩展和系统自动填充。
- 密码泄露、弱密码和重复密码健康分析。
- TestFlight/App Store 分发与真机签名。

### 5.4 后续专项测试

- 全量 VoiceOver、动态字体最大尺寸、Switch Control 和外接键盘。
- 网络断开、超时、限流和恶意图标响应。
- 超大损坏备份、模糊测试、长期压力和性能基准。
- Face ID/Touch ID 物理设备成功、失败和取消矩阵。
- Intel 物理 Mac 实机启动；当前已完成 x86_64 构建和二进制结构验证。

## 6. 最终发布判断

PocketPass V1.1(2) 的 macOS、iPhone 和 iPad 客户端在当前目标范围内已完成业务核心统一、主要功能实现、代表性设备适配与 10 轮回归。当前质量满足本地开发、个人使用和私有测试。

若扩大分发范围，发布前置条件为：Developer ID/公证或 iOS 开发者签名、真机生物认证回归、钥匙串/Secure Enclave 密钥封装，以及无障碍和异常输入专项测试。
