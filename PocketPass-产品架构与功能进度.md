# PocketPass｜口袋密码：产品架构与功能进度

更新日期：2026-09-21

当前版本：`V1.2(4)`

产品定位：Apple 平台本地优先账户密码管理器

## 一、平台与发布状态

| 平台 | 状态 | 最低系统 | 当前交付方式 |
|---|---|---|---|
| macOS | 已实现并完成回归 | macOS 26 | Universal DMG，支持拖入 Applications |
| iPhone | 已实现并完成适配 | iOS 26 | Xcode 工程、本地模拟器/真机开发构建 |
| iPad | 已实现并完成自适应分栏 | iPadOS 26 | 与 iPhone 共用一个 Universal iOS target |

macOS 安装包：`dist/PocketPass-V1.2(4)-20260921.dmg`

SHA-256：`aead82bee37e433c7b6b898ae96b38b71c03e122e0ab4c21a6feb6c8e3ce4bc1`

iOS / iPadOS 未签名安装包：`dist/PocketPass-V1.2(4)-unsigned.ipa`

iOS IPA SHA-256：`9d520591c2ea4d3ee6f2ccdd2549db6635a796e70c69be8b2fa4585a7d8a771e`

macOS Bundle ID：`com.loongyu.pocketpass`

iOS/iPadOS Bundle ID：`com.loongyu.pocketpass.ios`

首次启动只创建六个默认分类，不包含预置账户、密码或其他样例数据。

## 二、统一产品架构

### 2.1 分层结构

| 层级 | 实现 |
|---|---|
| 共享业务核心 | `PocketPassCore` Swift Package library |
| 数据模型 | `VaultSnapshot → VaultItem → LoginAccount → CustomField`，另含分类、标签、自定义图标和图片附件 |
| 数据格式 | `VaultSnapshot.formatVersion = 3`，为缺失新字段的旧 JSON 提供默认值解码 |
| 共享状态与规则 | `PocketPassCore.VaultStore` 统一 CRUD、搜索、标签、分类排序、回收站、导入合并、资源上限和持久化 |
| 本地存储 | JSON 编码后使用 AES-GCM 加密，原子写入本地应用数据目录 |
| macOS 界面 | SwiftUI + 少量 AppKit；薄适配器转发到共享核心 |
| iPhone/iPad 界面 | SwiftUI；Compact 使用 TabView，Regular 使用 NavigationSplitView |
| 生物认证 | macOS/iOS/iPadOS 均使用 LocalAuthentication 与设备所有者认证策略 |
| 数据迁移 | `.pocketpass` 加密备份、JSON、CSV、Markdown |
| 网络图标 | 共享 App Store Search API 与网站 favicon/Apple Touch Icon 服务 |

三端不再分别维护账户、分类、标签、回收站、导入去重和密码生成逻辑；这些规则位于 `Sources/PocketPassCore/`。平台层仅负责窗口、导航、文件选择、相册、剪贴板和生物认证等系统交互。

### 2.2 本地加密与数据安全

- 密码库文件使用 AES-GCM 加密，不保存账户密码明文。
- 首次创建密码库时生成随机 32 字节本地密钥。
- 若加密密码库存在但密钥缺失，加载会明确失败，不会用空数据覆盖原密码库。
- 密钥、密文损坏或保存失败时进入写入保护并回滚内存变更，避免覆盖最后一次有效快照。
- 保存采用原子写入；30 天过期回收站清理后立即持久化。
- 加密备份使用随机盐、PBKDF2-HMAC-SHA256 200,000 次派生密钥，再使用 AES-GCM 加密。
- 剪贴板复制内容在 30 秒后自动清除；若期间剪贴板已变化则不覆盖。
- 当前不会读取系统钥匙串中的账户或密码。
- 当前本地数据密钥仍与密码库位于同一应用数据域，尚未升级到钥匙串或 Secure Enclave 封装。

### 2.3 统一资源约束

- 每个账户最多 5 张附件。
- 单张附件最大 2,000,000 字节。
- 账户图标、分类图标和自定义图标最终数据最大 2,000,000 字节。
- 自定义图标库最多 100 枚，并按图片内容去重。
- iOS/iPadOS 图片在入库前缩放至最长边不超过 512 点并压缩；共享核心会再次执行边界校验。
- 导入数据中的孤立分类会自动修复，超限附件和图标不会写入密码库。
- 导入文件最大 250 MB；远程图标按响应流限制下载体积，图片解码前校验像素尺寸。

## 三、品牌与界面规范

- 中文品牌：口袋密码；英文品牌：PocketPass。
- App 图标使用黑色 Apple 风格圆角底板与黄色立体锁。
- 应用内标识使用同款透明背景黄色立体锁，不增加边框或额外底板。
- 主品牌色：`#FDBF02`；全局主操作、选中导航和状态强调使用同一金黄色系。
- 深色、亮色、跟随系统三种外观均已支持。
- 简体中文与 English 可在应用内即时切换并持久保存。
- 主要按钮使用胶囊样式；新建和编辑统一使用“保存 / Save”。
- 分类导航使用四宫格图标，避免与收藏夹图标混淆。

## 四、三端已完成功能

### 4.1 账户与字段

- 新建、编辑、软删除和永久删除账户项目。
- 一个项目可包含多个登录账号；每个账号有独立用户名、密码和自定义字段。
- 常用模板：账户、密码、手机号、邮箱、ID、昵称、注册时间和网址。
- 自定义字段支持自定义名称、普通/秘密类型、编辑和删除。
- 网址作为自定义字段使用，同时兼容旧数据中的独立网站字段。
- 支持备注、共享标签和最多 5 张图片附件。
- 账户详情支持密码显隐、字段复制及复制字段名称提示。
- 支持按名称、旧网站字段、标签、用户名、自定义字段名称或内容搜索。
- 支持按最近修改、名称和分类排序。
- 首页支持多选、全选和批量移入回收站。

### 4.2 分类、标签与图标

- 默认社交、游戏、银行、工作、购物、其他六个分类。
- 分类支持新增、编辑、删除、拖拽排序和持久化；始终至少保留一个分类。
- 删除分类时，其账户自动迁移到剩余分类。
- 分类支持预设颜色和原生任意颜色选择器。
- 标签可统一创建、重命名、删除，并被多个账户复用。
- 账户和分类共用四来源图标选择器：图标库、App Store、网址和照片/本地图片。
- App Store 默认中国区，可切换中国、美国、日本、英国、香港、德国、韩国、法国、西班牙。
- 自定义图标库支持批量添加、内容去重、删除和跨账户/分类复用。

### 4.3 回收站与密码生成

- 回收站保留 30 天，支持逐项/批量恢复和永久删除。
- 首页及账户密码字段均可打开密码生成器。
- 密码长度 8–64 位；小写为基础，可选大写、数字和符号。
- 使用 `SystemRandomNumberGenerator`，并保证每种已启用字符类型至少出现一次。
- 默认排除易混淆字符；支持重新生成、复制和直接使用。

### 4.4 导入、导出与备份

- 支持 `.pocketpass` 完整加密备份。
- 支持 JSON、CSV、Markdown 导入与导出。
- `.pocketpass` 与 JSON 完整保留账户图标、分类图标和自定义图标库；CSV/Markdown 保持纯文本。
- 导入/导出显示进度和处理结果。
- 同 UUID 项目按最后修改时间较新版本优先。
- 不同 UUID 但语义内容相同的项目会跳过，重复导入保持幂等。
- 分类按 UUID 或名称合并；自定义图标按图片内容去重。
- JSON 与 `.pocketpass` 可保留完整结构；CSV/Markdown 是可读的有损交换格式。
- 兼容缺失新字段的旧版 JSON 和旧版 Markdown 多账号结构。

### 4.5 应用锁、设置与本地化

- 应用锁使用 Touch ID、Face ID 或设备密码，不提供独立主密码。
- 自动锁定支持立即、1、5、15、30 分钟，默认 5 分钟。
- 锁定状态覆盖整个业务界面，认证由系统弹窗完成。
- 设置包含安全、iCloud 状态、通用、内容、数据和关于。
- 简体中文与 English 的静态界面和运行时数量/结果提示均已适配。
- iPad 使用常驻侧栏和内容区；iPhone 使用底部导航和紧凑布局。

## 五、当前测试与构建能力

- 标准 Swift Testing target：`Tests/PocketPassCoreTests/`。
- 当前 19 项共享核心自动测试全部通过。
- 覆盖加密持久化、密钥缺失保护、回收站、30 天清理、分类迁移/排序、标签、图标库、资源边界、搜索、密码生成、旧数据兼容、四格式往返和重复导入。
- `xcodebuild analyze` 已通过。
- iPhone 17 Pro Max、iPhone 17e、iPad mini (A17 Pro)、iPad Pro 13-inch (M5) 模拟器完成代表性尺寸验证。
- macOS Release 构建、Universal Binary、临时签名结构、DMG 完整性和拖拽安装结构已通过。
- V1.1 三端验证记录见 `PocketPass-V1.1-三端功能测试报告.md`；V1.2 已重新通过共享核心、iPhone、iPad、macOS Release 和 DMG 检查。

## 六、未完成与已知限制

### 6.1 暂缓功能

- iCloud/CloudKit 同步及多设备冲突历史。
- Safari、Chrome、Edge、Firefox 扩展。
- Password AutoFill Extension 和系统自动填充。
- 泄露密码检测、弱密码/重复密码健康分析和过期提醒。

iCloud 设置目前仅说明“暂未开放”，没有 CloudKit 容器或同步代码；这是按当前开发阶段有意保留的状态。

### 6.2 安全与发布限制

- 本地密钥尚未由钥匙串或 Secure Enclave 封装。
- 尚未进行独立密码学与安全审计。
- macOS 安装包为临时签名，未使用 Developer ID，也未完成 Apple 公证；其他 Mac 可能触发 Gatekeeper 提示。
- iOS/iPadOS 当前为本地开发工程，尚未配置付费开发者团队、TestFlight 或 App Store 发布。

### 6.3 工程增强项

- 尚未接入持续集成、UI 自动化、性能基准、模糊测试和崩溃收集。
- VoiceOver、动态字体极限值、外接键盘和 Switch Control 仍需专项人工审查。
- 收藏字段存在于模型，但尚未形成完整收藏入口和筛选页面。
- CSV/Markdown 导入属于有损格式，仍可继续增加逐项预览、冲突比较和撤销导入。

## 七、建议后续顺序

1. 配置 Apple Developer 账户，完成 iOS/iPadOS 真机与 TestFlight 分发。
2. 将本地密钥封装到钥匙串/Secure Enclave，并制定密钥恢复策略。
3. 建立 CI、UI 自动化、性能与异常文件测试。
4. 完成 VoiceOver、动态字体和全键盘操作专项适配。
5. 实现密码健康检查与泄露检测。
6. 在具备 CloudKit 权限后实现端到端加密同步和冲突记录。
7. 最后开发浏览器扩展与系统自动填充。
