# PocketPass｜口袋密码：产品架构与功能进度

当前产品版本为 `PocketPass｜口袋密码 V1.0(1)`，定位为 macOS 本地优先账户密码管理器。

## 一、当前产品架构

| 层级 | 当前实现 |
|---|---|
| 应用层 | 原生 Swift 6、SwiftUI，最低支持 macOS 26 |
| 导航结构 | 首页、分类、回收站、设置 |
| 状态管理 | `@Observable VaultStore` 统一管理账户、分类、标签、筛选、锁定状态 |
| 数据模型 | `VaultSnapshot → VaultItem → LoginAccount → CustomField` |
| 本地存储 | JSON 序列化后使用 AES-GCM 加密，保存到 Application Support |
| 身份验证 | LocalAuthentication，支持 Touch ID 或 macOS 设备密码 |
| 网络服务 | App Store Search API、网站 favicon 请求 |
| 数据迁移 | 加密备份、JSON、CSV、Markdown |
| 发布方式 | Universal Binary，支持 Apple Silicon 与 Intel，DMG 拖拽安装 |
| 品牌标识 | 英文 `PocketPass`，中文“口袋密码” |
| 代码管理 | GitHub 私有仓库 `LoongYu/PocketPass` |

数据结构已提前考虑 iOS、iPadOS 和 CloudKit 的兼容性，主要实体均支持 `Codable`、UUID 和修改时间。

## 二、已完成功能

### 账户管理

- 新建、编辑、删除账户项目。
- 一个账户项目包含多个登录账号。
- 每个登录账号具有独立用户名、密码和自定义字段。
- 支持常用字段模板和任意自定义字段。
- 自定义字段支持删除及秘密字段显示。
- 账户详情可显示、隐藏、复制密码。
- 复制后显示具体字段名称。
- 首页搜索支持名称、网址、标签、用户名和自定义字段。
- 支持按最近修改、名称、分类排序。

### 分类与标签

- 默认提供社交、游戏、银行、工作、购物、其他分类。
- 支持新增、编辑、删除分类。
- 删除分类后，账户自动迁移至其他可用分类。
- 分类支持自定义颜色。
- 分类支持系统图标及图片图标。
- 分类页面一行显示五张卡片，数量过多时可以滚动。
- 首页前五个分类直接显示，其余分类收纳到更多菜单。
- 分类导航统一使用四宫格图标，与收藏、标签和文件夹语义明确区分。
- 分类卡片支持鼠标按住拖拽自定义顺序，并同步影响首页分类顺序。
- 首页排序支持最近修改、添加时间、名称和分类。
- 设置中的自定义图标库支持批量上传、删除和跨账户/分类复用，最多保存 100 个。
- 账户列表支持批量选择、全选及批量移入回收站。
- 回收站支持批量恢复和批量永久删除，并保留二次确认。
- 标签可统一创建、重命名和删除。
- 标签可以被多个账户共同使用。

### 图标系统

- 内置单色 SF Symbols 图标库。
- 默认填充分类/账户主题底色。
- 支持 App Store 图标搜索。
- 默认搜索中国区。
- 支持中国、美国、日本、英国、香港、德国、韩国、法国、西班牙九个地区。
- 支持根据网址读取 Apple Touch Icon 或 favicon。
- 支持从相册、本地文件选择图片。
- 网络及相册图标限制为最大 5 MB。
- 新建账户与分类共用相同图标选择逻辑。

### 图片附件

- 一个账户最多保存五张图片。
- 单张图片最大 5 MB。
- 支持添加、预览和删除附件。
- 图片与账户数据一起进入本地加密密码库和备份文件。

### 安全与本地存储

- 密码库使用 AES-GCM 加密。
- 每次保存采用原子写入。
- 本地密钥文件限制为当前用户读取。
- 不读取 macOS 钥匙串内容。
- 应用锁支持 Touch ID 或设备密码。
- 支持立即、1、5、15、30 分钟自动锁定。
- 锁定界面覆盖整个应用内容。
- 默认离开应用五分钟后锁定。
- 当前所有业务数据仅保存在本机。

### 密码生成器

- 首页总览提供独立的密码生成入口。
- 支持 8 至 64 位密码长度。
- 支持大写字母、数字和符号组合。
- 使用系统安全随机数生成密码，并保证选中的字符类型至少出现一次。
- 支持复制密码或直接写入新建、编辑账户的密码字段。

### 回收站

- 删除账户后进入回收站。
- 默认保留 30 天。
- 支持恢复账户。
- 支持立即永久删除。
- 启动时自动清理超过 30 天的数据。

### 导入、导出及备份

- 支持 `.pocketpass` 加密备份。
- 加密备份使用独立的六位以上密码。
- 支持 JSON 导入导出。
- 支持 CSV 导入导出。
- 支持 Markdown 导入导出。
- 导入、导出显示进度条和处理数量。
- 导入结果显示新增、更新、跳过和登录账号数量。
- 相同 UUID 按最后修改时间较新的数据优先。
- 内容完全相同的数据会跳过，防止重复导入。
- 兼容旧版 `pockit` 备份类型。

### 界面与发布

- 深色与亮色统一视觉风格。
- 按钮主要统一为胶囊样式。
- 支持深色、亮色与跟随系统外观。
- 原生支持简体中文与 English，可在设置中即时切换。
- 新建、编辑、图标、标签、字段等窗口已完成。
- 首次启动不包含测试账户。
- 已生成 `PocketPass-2026081501.dmg`。
- DMG 支持拖拽“口袋密码”到 Applications。
- 已发布到私有 GitHub Release。

## 三、部分完成或需要继续加强

- 收藏数据字段和底层切换逻辑已存在，但目前没有完整的收藏管理页面和明确操作入口。
- 修改时间冲突策略已用于导入，但暂时没有冲突记录或手动比较界面。
- 本地密码库已加密，但加密密钥与密码库保存在同一应用数据目录，适合当前 Demo，尚未达到专业密码管理器的安全强度。
- 导入导出具备基础校验，但还需要覆盖损坏文件、超大文件、旧版本迁移等更多异常场景。
- 已有基础验证程序，但还没有完整的单元测试、UI 自动化测试和安全测试体系。

## 四、尚未完成功能

### 云端与多设备

- iCloud/CloudKit 同步。
- 数据端到端加密同步。
- 多设备冲突记录。
- iPhone 客户端。
- iPad 客户端。

iCloud 设置目前只是状态说明，没有实际同步代码。

### 密码能力

- 密码强度评分。
- 弱密码检查。
- 重复密码检查。
- 密码过期提醒。
- 泄露密码检测。

### 浏览器和系统集成

- Safari 扩展。
- Chrome、Edge、Firefox 扩展。
- macOS Password AutoFill Extension。
- 网页自动填充。
- 登录后自动保存账户。

### 发布与产品化

- Apple Developer ID 正式签名。
- Apple 公证与 Gatekeeper 免提示安装。
- 自动更新。
- 崩溃收集和诊断。
- 完整无障碍及键盘操作。
- 独立安全审计。
- 定时备份和历史备份管理。

## 五、建议后续开发顺序

1. 本地安全架构加固。
2. 密码强度与安全检查。
3. Developer ID 正式签名和公证。
4. iCloud/CloudKit 加密同步。
5. iOS 与 iPadOS 客户端。
6. 浏览器扩展和系统自动填充。
