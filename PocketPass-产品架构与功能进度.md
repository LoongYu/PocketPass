# PocketPass｜口袋密码：产品架构与功能进度

更新日期：2026-08-15

当前正式版本：`V1.1(2)`

Git 标签：`v1.1.0`

产品定位：macOS 本地优先账户密码管理器

## 一、版本与发布状态

| 项目 | 当前状态 |
|---|---|
| 中文品牌 | 口袋密码 |
| 英文品牌 | PocketPass |
| Bundle ID | `com.loongyu.pocketpass` |
| 最低系统 | macOS 26 |
| 处理器架构 | Apple Silicon 与 Intel Universal Binary |
| 安装包 | `PocketPass-2026081502.dmg` |
| 安装方式 | 打开 DMG，将“口袋密码”拖入 Applications |
| GitHub | 私有仓库 `LoongYu/PocketPass`，已发布 V1.1 Release |
| 签名状态 | 临时签名，可本地安装；尚未使用 Developer ID 签名或 Apple 公证 |
| 安装包 SHA-256 | `bb5b05cab33685112362d2d6490932f861e60e712a4ba7f510d9276e15b6a5f1` |

首次启动只创建六个默认分类，不包含演示账户、密码或其他测试数据。

## 二、当前产品架构

| 层级 | 当前实现 |
|---|---|
| 应用层 | Swift 6、SwiftUI、少量 AppKit 桥接 |
| 导航结构 | 首页、分类、回收站、设置 |
| 状态管理 | `@Observable VaultStore` 统一管理账户、分类、标签、自定义图标、筛选、偏好与锁定状态 |
| 核心数据模型 | `VaultSnapshot → VaultItem → LoginAccount → CustomField` |
| 数据格式版本 | `VaultSnapshot.formatVersion = 3` |
| 数据兼容基础 | 模型使用 `Codable`、UUID 和修改时间，为后续 iOS、iPadOS 与同步层保留迁移基础；目前并非可直接启用的 CloudKit 架构 |
| 本地存储 | 数据序列化为 JSON 后使用 AES-GCM 加密，原子写入 `~/Library/Application Support/口袋密码/` |
| 本地密钥 | 随机生成 32 字节密钥，权限设置为当前用户读写；目前与密码库位于同一应用数据目录 |
| 应用锁 | LocalAuthentication，支持 Touch ID 或 macOS 设备密码 |
| 偏好设置 | 主题、语言、排序方式、应用锁与自动锁定时间使用 UserDefaults 保存 |
| 网络服务 | 按用户操作请求 App Store Search API、Apple Touch Icon 或网站 favicon |
| 数据迁移 | `.pocketpass` 加密备份、JSON、CSV、Markdown |
| 本地化 | 简体中文为默认语言，原生提供 English 本地化资源 |
| 发布构建 | 脚本生成 Universal App、图标资源、临时签名和拖拽安装 DMG |

## 三、品牌与界面规范

- App 图标使用黑色圆角底板加黄色立体锁。
- 应用内品牌标识使用同款透明背景黄色立体锁，不额外添加边框或底板。
- Logo 主金色为 `#FDBF02`。
- 全局主操作按钮、选中菜单、导航状态和安装界面使用同一品牌色系。
- 简体中文界面显示“口袋密码”，English 界面显示“PocketPass”。
- macOS 应用菜单的品牌名称随应用内语言即时切换，无需重启。
- 支持深色、亮色和跟随系统三种外观。
- 主要按钮统一为胶囊样式，新建与编辑窗口统一使用“保存”按钮。

## 四、已完成功能

### 4.1 账户管理

- 新建、编辑账户项目，并通过软删除移入回收站。
- 一个账户项目可以包含多个登录账号。
- 每个登录账号拥有独立用户名、密码和自定义字段。
- 提供账号、密码、手机号、邮箱、ID、昵称、注册时间、网址等常用字段模板。
- 支持创建任意名称的自定义字段，并可设置普通或秘密字段。
- 编辑账户时支持删除、重命名自定义字段以及删除多余登录账号。
- 网址已经作为账户自定义字段使用；旧数据中的独立网址会在编辑时迁移到“网址”字段。
- 账户详情显示分类名称及账户字段，不默认展示备注、标签、附件等其他信息。
- 支持显示或隐藏密码，以及复制用户名、密码和自定义字段。
- 复制后提示具体字段名称；剪贴板内容在 30 秒后自动清除，若用户期间复制了其他内容则不覆盖。
- 首页支持按名称、旧版网址、标签、用户名和自定义字段名称或内容搜索。
- 首页支持按最近修改、名称和分类排序。

### 4.2 批量管理与回收站

- 首页账户列表支持进入批量选择模式、逐项选择和全选。
- 支持批量移入回收站。
- 回收站支持逐项或批量恢复。
- 回收站支持逐项或批量永久删除，并提供二次确认。
- 删除项目默认保留 30 天，启动时自动清理过期项目。

### 4.3 分类与标签

- 默认提供社交、游戏、银行、工作、购物、其他六个分类。
- 支持新增、编辑和删除分类；至少保留一个分类。
- 删除分类后，该分类下的账户自动迁移到“其他”或剩余分类。
- 分类支持预设颜色和系统取色器自选颜色。
- 分类支持 SF Symbols、App Store、网址和本地图片图标。
- 分类页一行显示五张卡片，数量过多时在区域内滚动。
- 首页显示前五个分类，其余分类收纳进“更多”菜单。
- 分类卡片支持鼠标按住拖拽自定义顺序，顺序会持久保存并同步影响首页分类顺序。
- 分类导航使用四宫格图标，避免与收藏夹图标混淆。
- 标签可在统一标签管理页创建、重命名和删除。
- 标签可被多个账户共同使用。

### 4.4 图标系统

- 内置单色 SF Symbols 图标库，并使用账户或分类主题底色展示。
- 自定义图标库支持批量上传、删除以及跨账户和分类复用。
- 自定义图标库最多保存 100 个图标，按图片内容去重。
- 支持 App Store 图标搜索，默认使用中国区。
- App Store 搜索支持中国、美国、日本、英国、香港、德国、韩国、法国、西班牙九个常用区域。
- 支持从网址优先获取 Apple Touch Icon，其次获取 favicon。
- 支持从本地相册或图片文件选择图标。
- 下载或导入的单个图标最大 5 MB。
- 新建账户、编辑账户、新增分类和编辑分类共用同一套图标选择逻辑。

### 4.5 图片附件

- 一个账户项目最多保存五张图片。
- 单张图片最大 5 MB。
- 支持添加、预览和删除附件。
- 附件随账户数据进入本地加密密码库和完整加密备份。

### 4.6 安全与本地存储

- 本地密码库使用 AES-GCM 加密。
- 保存操作采用原子写入，并使用系统文件保护写入选项。
- 本地密钥随机生成，文件权限限制为当前用户读写。
- 应用不会读取 macOS 钥匙串中的密码或机密信息。
- 应用锁支持 Touch ID 或 macOS 设备密码，不设置独立主密码。
- 自动锁定支持立即、1、5、15、30 分钟，默认离开应用五分钟后锁定。
- 锁定界面覆盖整个应用内容。
- 当前业务数据仅保存在本机；App Store 与网站图标请求只在用户主动搜索或获取图标时发生。

### 4.7 密码生成器

- 首页总览提供独立的密码生成入口。
- 新建和编辑账户的密码字段可直接打开生成器并写入结果。
- 支持 8 至 64 位密码长度。
- 小写字母为基础字符集，可选大写字母、数字和符号。
- 使用 `SystemRandomNumberGenerator`，并保证已选字符类型至少出现一次。
- 默认排除容易混淆的部分字母和数字。
- 支持重新生成、复制或直接使用生成结果。

### 4.8 导入、导出与备份

- 支持 `.pocketpass` 完整加密备份。
- 备份密码要求六位以上；使用随机盐、PBKDF2-HMAC-SHA256 迭代 200,000 次派生密钥，再使用 AES-GCM 加密。
- 支持 JSON、CSV、Markdown 导入和导出。
- 导入、导出显示进度条、处理数量和结果统计。
- 导入结果区分新增、更新、跳过以及导入的登录账号数量。
- 相同 UUID 的账户按最后修改时间较新的版本优先。
- 不同 UUID 但语义内容完全相同的账户会跳过，使重复导入保持幂等。
- 导入分类按 UUID 或分类名称合并，避免重复创建同名分类。
- 自定义图标按 UUID 或图片内容去重。
- 兼容旧版 `pockit` 加密备份类型。
- JSON 和 `.pocketpass` 可保留完整数据；CSV、Markdown 主要用于可读交换，不完整保留图标、附件及所有字段元数据。
- JSON、CSV 和 Markdown 为明文格式，界面会提示用户妥善保管。

### 4.9 设置、语言与发布

- 设置页包含安全、iCloud 状态、通用、内容、数据和关于六个区域。
- 通用设置支持主题、应用语言和首页排序。
- 内容设置提供标签管理、自定义图标库和分类管理入口。
- 数据设置提供导入以及导出与本地加密备份入口。
- 原生支持简体中文与 English，切换后界面和应用菜单品牌名即时更新并记住选择。
- 已发布 `PocketPass V1.1` GitHub 私有 Release。
- DMG 支持将“口袋密码”拖入 Applications 安装。

## 五、已有验证能力

当前仓库包含六个独立验证程序，但尚未接入 Swift Package 的标准测试 target 或持续集成：

- `CompletedFeaturesValidation.swift`：综合验证账户、分类、标签、搜索、排序、回收站、本地加密和持久化。
- `SmokeTests.swift`：验证 JSON、CSV、加密备份、自定义字段、附件和自定义图标的基本往返。
- `MarkdownTransferValidation.swift`：验证 Markdown 多账户解析及导出后重新导入。
- `FirstLaunchAndImportValidation.swift`：验证首次启动无测试账户、分类顺序持久化、自定义图标去重和重复导入幂等。
- `PasswordGeneratorValidation.swift`：验证多种长度及字符集约束。
- `RemoteIconSmoke.swift`：验证 App Store 搜索、图标下载和网站 favicon 网络请求。

正式构建脚本已验证：

- Apple Silicon 与 Intel 双架构合并。
- App 包结构与临时签名有效。
- DMG 校验和有效。
- 包内不包含账户数据库或测试数据。

## 六、部分完成或需要加强

### 6.1 收藏能力

- `VaultItem.isFavorite` 字段和 `toggleFavorite` 底层逻辑已经存在。
- 目前没有收藏导航、收藏筛选页面或稳定的收藏操作入口，因此不能视为完整功能。

### 6.2 本地安全架构

- 密码库已经加密，但本地密钥与密码库保存在同一应用数据目录。
- 尚未使用 Secure Enclave 或钥匙串保护数据加密密钥；当前实现适合个人 Demo 验证，不等同于经过审计的专业密码管理器。
- 应用锁保护界面访问，不替代 FileVault、macOS 用户账户安全或独立的密码库主密钥。

### 6.3 数据兼容与冲突处理

- 修改时间较新优先的策略已经用于导入。
- 尚无冲突历史、手动比较、撤销导入或逐项选择合并界面。
- CSV 与 Markdown 属于有损交换格式，复杂自定义字段类型、附件、图标等不能完整往返。
- 仍需补充损坏文件、超大文件、跨版本迁移和异常中断恢复测试。

### 6.4 测试与质量

- 已有基础验证程序，但尚未形成 XCTest/Swift Testing 自动测试套件。
- 尚无持续集成、完整 UI 自动化、性能测试、模糊测试和安全测试。
- 无障碍标签已有部分基础支持，但尚未完成全界面 VoiceOver 与键盘操作审查。

### 6.5 本地化

- 主要界面已支持中英文切换。
- macOS 应用菜单中的品牌名可随应用语言切换；系统提供的“文件、编辑、窗口”等标准菜单仍由 macOS 系统语言管理。
- 仍需进行完整英文文案、截断、复数和无障碍朗读校对。

## 七、尚未完成功能

### 7.1 云端与多设备

- iCloud/CloudKit 同步。
- 端到端加密的多设备同步。
- 多设备冲突记录与人工处理界面。
- iPhone 客户端。
- iPad 客户端。

iCloud 设置当前仅显示“暂未开放”，没有 CloudKit 容器、权限或同步代码。

### 7.2 密码安全能力

- 密码强度评分。
- 弱密码检查。
- 重复密码检查。
- 密码过期提醒。
- 泄露密码检测。
- 密码健康总览与整改建议。

### 7.3 浏览器和系统集成

- Safari 扩展。
- Chrome、Edge、Firefox 扩展。
- macOS Password AutoFill Extension。
- 网页和系统登录框自动填充。
- 登录后自动保存账户。

### 7.4 发布与产品化

- Apple Developer ID 正式签名。
- Apple 公证及 Gatekeeper 免提示安装。
- 自动更新。
- 崩溃收集和诊断。
- 完整键盘操作和无障碍适配。
- 独立安全审计。
- 定时备份、历史备份和恢复点管理。

## 八、建议后续开发顺序

1. 将验证程序接入标准测试 target，并建立基础持续集成。
2. 加固本地密钥保护与数据恢复机制。
3. 增加密码强度、弱密码和重复密码检查。
4. 完善无障碍、键盘操作和中英文文案校对。
5. 在取得 Apple Developer 账户后完成 Developer ID 签名与公证。
6. 设计并实现 CloudKit 端到端加密同步及冲突记录。
7. 开发 iPhone 与 iPad 客户端。
8. 开发浏览器扩展和系统自动填充。
