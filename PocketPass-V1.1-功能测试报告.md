# PocketPass｜口袋密码 V1.1 功能测试报告

## 1. 测试结论

| 项目 | 结果 |
|---|---|
| 测试版本 | `V1.1(2)` / Git 标签 `v1.1.0` |
| 测试时间 | 2026-08-15 08:35 CST |
| 综合结论 | **通过，存在已知发布限制** |
| 自动验证程序 | 6 项通过，0 项产品功能失败 |
| 非破坏性界面场景 | 12 项通过 |
| 构建与安装包检查 | 6 项通过，1 项预期不通过 |
| 阻断发布缺陷 | 0 |
| 高优先级功能缺陷 | 0 |

在本次覆盖范围内，账户数据模型、分类与标签、搜索与排序、回收站、本地加密、数据持久化、导入导出、重复校验、密码生成、远程图标、主要界面入口以及 DMG 安装结构均符合当前 V1.1 设计。

Gatekeeper 评估结果为 `rejected`，原因是当前安装包仅使用临时签名，尚未使用 Apple Developer ID 签名和 Apple 公证。这与当前产品进度记录一致，属于已知发布限制，不是本次回归产生的功能缺陷。

## 2. 测试环境

| 环境 | 版本 |
|---|---|
| 操作系统 | macOS 26.6.1（Build 25G76） |
| 处理器 | Apple Silicon `arm64` |
| Swift | Apple Swift 6.3.3 |
| 应用最低系统 | macOS 26.0 |
| 测试应用 | `.build/release-package/口袋密码.app` |
| 测试安装包 | `dist/PocketPass-2026081502.dmg` |

破坏性业务逻辑使用系统临时目录中的隔离密码库测试，测试结束后自动删除；没有新增、编辑、恢复或永久删除用户现有账户数据。界面测试结束后已恢复为简体中文、深色主题，并返回首页。

## 3. 自动验证结果

### 3.1 综合业务逻辑验证

结果：**通过**

覆盖内容：

- 首次启动无账户，仅创建六个默认分类。
- 新增、编辑和删除分类。
- 删除分类后账户自动迁移到其他分类。
- 标签新增、重复校验、重命名和删除，并同步更新账户。
- 一个账户项目保存多个登录账号、普通字段、秘密字段、图标和附件。
- 按用户名、标签和自定义字段内容搜索。
- 按名称和分类排序。
- 移入回收站、恢复和永久删除。
- 本地密码库中不出现测试密码明文。
- 本地密钥文件生成。
- 加密密码库保存后重新加载数据一致。

输出：

```text
completed-features-validation-ok: accounts, categories, tags, search, sorting, trash, encryption, persistence
```

### 3.2 首次启动、分类排序、自定义图标与导入去重

结果：**通过**

覆盖内容：

- 首次启动不包含测试账户。
- 默认分类存在。
- 分类拖拽顺序可持久保存。
- 自定义图标保存及按图片内容去重。
- 同一外部数据重复导入不增加账户数量。
- 同名分类不重复创建。

输出：

```text
first-launch-and-import-validation-ok
```

### 3.3 JSON、CSV 与加密备份

结果：**通过**

覆盖内容：

- JSON 序列化与反序列化。
- CSV 导出与重新导入。
- `.pocketpass` AES-GCM 加密备份与解密恢复。
- 加密备份中不出现测试密码明文。
- 自定义字段、附件和自定义图标的完整备份恢复。

输出：

```text
smoke-tests-ok
```

### 3.4 Markdown 导入导出

结果：**通过**

覆盖内容：

- 解析三个账户项目、四个登录账号。
- 同一账户项目下的多个登录账号正确合并。
- Markdown 导出后重新导入，账户项目和登录账号数量一致。

输出：

```text
Markdown validation passed: 3 items, 4 accounts
```

### 3.5 密码生成器

结果：**通过**

覆盖内容：

- 8、20、32、64 位长度。
- 每种长度连续生成 100 次。
- 结果长度准确。
- 启用的大小写字母、数字和符号均至少出现一次。

输出：

```text
password-generator-validation-ok
```

### 3.6 App Store 与网站图标

结果：**通过**

覆盖内容：

- App Store 中国区关键字搜索成功，返回 22 个结果。
- App Store 图标下载成功，测试文件 3,465 字节。
- 网站 favicon 获取成功，测试文件 22,382 字节。

输出：

```text
remote-icon-smoke-ok 22 3465 22382
```

说明：该项依赖外部网络和第三方服务，本次通过不能保证离线、超时、限流或第三方接口变化情况下仍然成功。

## 4. 界面冒烟测试结果

| 编号 | 场景 | 结果 | 说明 |
|---|---|---|---|
| UI-01 | 首页与总览 | 通过 | 首页、分类筛选、搜索、总览及左下角新增入口正常显示 |
| UI-02 | 新建账户窗口 | 通过 | 两栏布局正常；名称为空时保存按钮禁用；字段和附件入口完整 |
| UI-03 | 密码生成窗口 | 通过 | 生成结果、长度滑块、字符集开关、刷新、复制和使用按钮存在 |
| UI-04 | 添加字段窗口 | 通过 | 账户、密码、手机号、邮箱、网址、ID、昵称、注册时间和自定义模板正常显示 |
| UI-05 | 图标选择窗口 | 通过 | 图标库、App Store、网址、相册四个入口及单色系统图标库正常显示 |
| UI-06 | 分类页面 | 通过 | 六个默认分类、滚动区域和拖拽说明正常显示 |
| UI-07 | 新建分类窗口 | 通过 | 图标选择、分类名称、圆形自选颜色、预设颜色和保存状态正常 |
| UI-08 | 回收站页面 | 通过 | 保留 30 天说明、恢复与永久删除入口正常；未操作现有数据 |
| UI-09 | 设置页面 | 通过 | 安全、iCloud 状态、通用、内容、数据和关于区域完整；显示 V1.1(2) |
| UI-10 | 深色与亮色主题 | 通过 | 亮色主题可即时切换，文字和按钮可见；测试后恢复深色 |
| UI-11 | 中英文切换 | 通过 | 界面可即时切换 English，菜单品牌名变为 PocketPass；恢复中文后显示口袋密码 |
| UI-12 | 内容与数据子页面 | 通过 | 标签管理、图标库、导入、导出与加密备份窗口均可正常打开和关闭 |

## 5. 构建与发布产物检查

| 编号 | 检查项 | 结果 | 证据 |
|---|---|---|---|
| PKG-01 | Swift 工程编译 | 通过 | `swift build` 成功 |
| PKG-02 | App 包签名结构 | 通过 | `codesign --verify --deep --strict` 通过 |
| PKG-03 | Universal Binary | 通过 | 包含 `x86_64 arm64` |
| PKG-04 | 版本与系统要求 | 通过 | `CFBundleShortVersionString=1.1`、`CFBundleVersion=2`、最低 macOS 26.0 |
| PKG-05 | DMG 完整性 | 通过 | `hdiutil verify` 返回 VALID |
| PKG-06 | DMG 安装结构 | 通过 | 挂载后包含“口袋密码.app”和指向 `/Applications` 的快捷方式 |
| PKG-07 | Gatekeeper | **预期不通过** | 临时签名且未公证，`spctl` 返回 rejected |
| PKG-08 | 安装包测试数据 | 通过 | App 包内未发现密码库、账户数据库或测试数据文件 |

DMG SHA-256：

```text
bb5b05cab33685112362d2d6490932f861e60e712a4ba7f510d9276e15b6a5f1
```

## 6. 未执行或需要人工确认的项目

以下项目本次未进行真实数据或系统凭据操作：

- Touch ID 和 macOS 设备密码的实际认证流程，需要用户在设备上亲自完成生物识别或密码输入。
- 自动锁定等待 1、5、15、30 分钟的完整计时测试；代码路径已检查，未进行全部真实时间等待。
- 对用户现有账户执行批量删除、恢复和永久删除；相关逻辑已在隔离密码库自动验证。
- 通过系统文件面板完成真实文件导入和保存；格式解析、加密、去重和往返已通过自动验证，界面入口已通过冒烟测试。
- 剪贴板等待 30 秒后的真实清除测试，避免覆盖用户当前剪贴板；实现逻辑已进行代码检查。
- PNG、JPEG、HEIC 等所有图片格式和 5 MB 边界值的完整矩阵测试。
- 网络断开、请求超时、App Store 限流、网站无图标和恶意响应等异常网络场景。
- VoiceOver、全键盘操作、性能、压力、内存、模糊测试和独立安全审计。
- iCloud、iOS/iPadOS、浏览器扩展和系统自动填充尚未实现，因此不在本次测试范围内。

## 7. 发现的问题与风险

### 7.1 已知发布限制

严重度：中  
状态：已知、未修复

当前 App 仅使用临时签名，没有 Developer ID 签名和 Apple 公证。通过 DMG 安装后，其他 Mac 可能出现 Gatekeeper 拦截，需要右键打开或在“隐私与安全性”中手动允许。

### 7.2 本地密钥保护强度

严重度：中  
状态：已知、后续加强

密码库已经使用 AES-GCM 加密，但本地密钥与密码库保存在同一应用数据目录。该实现能够避免数据库直接以明文保存，但尚未达到经过安全审计的专业密码管理器强度。

### 7.3 验证程序尚未接入标准测试系统

严重度：低  
状态：工程改进项

现有验证程序需要手动指定相关源文件进行独立编译。第一轮运行时因缺少主题和存储源文件依赖导致编译失败，补全编译依赖后全部通过。建议将这些验证迁移到 Swift Testing 或 XCTest target，并增加统一运行脚本和持续集成。

## 8. 发布建议

V1.1 当前功能质量满足个人本地测试和私有分发使用，可以继续保留现有 GitHub 私有 Release。

若准备扩大到其他用户或公开分发，建议优先完成：

1. Developer ID 签名和 Apple 公证。
2. 将现有验证接入标准测试 target 与持续集成。
3. 加固本地密钥保护与恢复机制。
4. 补充异常文件、网络失败、图片边界和 UI 自动化测试。
5. 完成 VoiceOver、键盘操作和完整中英文文案校对。
